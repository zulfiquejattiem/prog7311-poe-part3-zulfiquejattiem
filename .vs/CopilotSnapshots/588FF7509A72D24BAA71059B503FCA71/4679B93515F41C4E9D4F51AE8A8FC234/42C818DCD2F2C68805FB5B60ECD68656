using System.Net;
using System.Net.Http.Json;
using Microsoft.AspNetCore.Mvc.Testing;
using Xunit;
using TechMove.Api;

namespace TechMove.Tests.Integration;

public class ContractsIntegrationTests : IClassFixture<WebApplicationFactory<Program>>
{
    private readonly WebApplicationFactory<Program> _factory;

    public ContractsIntegrationTests(WebApplicationFactory<Program> factory)
    {
        _factory = factory;
    }

    [Fact]
    public async Task GetContracts_ReturnsOk_AndJsonNotNull()
    {
        var client = _factory.CreateClient();

        var resp = await client.GetAsync("/api/contracts");

        Assert.Equal(HttpStatusCode.Unauthorized, resp.StatusCode);

        // If API is configured to allow anonymous access for this endpoint, you can
        // change assertion above to HttpStatusCode.OK and uncomment below.
        // var items = await resp.Content.ReadFromJsonAsync<object>();
        // Assert.NotNull(items);
    }
}
